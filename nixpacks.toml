[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python3 app.py"
```

**重要：检查 `requirements.txt`**

确保你的项目根目录有 `requirements.txt` 文件，内容应该包括：
```
Flask==3.0.0
Flask-Cors==4.0.0
Werkzeug==3.0.0
PyJWT==2.8.0